# Upsun configuration, generated using AI at: 2026-05-06T16:45:39.202Z
# AI can make mistakes. Please modify this file to suit your needs.
applications:
  app:
    type: nodejs:24
    build:
      flavor: none
    hooks:
      build: |
        set -ex
        # No dependencies or build steps detected, placeholder for future build commands
    web:
      commands:
        start: node index.js

routes:
  https://{default}/:
    type: upstream
    upstream: app:http