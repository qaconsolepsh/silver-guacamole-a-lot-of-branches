# Upsun configuration, generated using AI at: 2026-05-04T16:37:51.197Z
# AI can make mistakes. Please modify this file to suit your needs.
applications:
  app:
    type: nodejs:24
    build:
      flavor: none
    hooks:
      build: |
        set -ex
        # No dependencies or build steps detected, placeholder for future build commands
      deploy: |
        set -ex
        # No deploy steps detected
    web:
      commands:
        start: sleep infinity

# Single application with no detected build or runtime commands.
# Using nodejs:24 as a safe default runtime.
# Start command is 'sleep infinity' assuming no web server detected.
# No services or routes defined due to lack of information.